You are building a beautiful React Native Android onboarding screen for SensoryScope — an app that helps users find sensory-friendly places (restaurants, parks, venues) rated for noise, lighting, crowds, and accessibility.

GOAL: Create a polished, production-ready multi-step onboarding flow in React Native (Expo) for Android. The onboarding should feel premium, calm, and accessible — reflecting the app's core mission of sensory comfort.

SCREENS TO BUILD (in order):
1. Welcome — App logo, tagline 'Find your calm in the world', subtle animated background
2. What is SensoryScope? — Brief value prop with 3 illustrated feature cards (sensory ratings, maps, reviews)
3. How it works — 3-step visual walkthrough (Search, Read ratings, Go with confidence)
4. Sensory Preferences — Let user select their sensory sensitivities (noise, lighting, crowds, textures) as toggleable chips
5. Permissions — Request location permission with friendly illustration and explanation
6. You're all set! — Celebration screen with CTA button 'Start Exploring'

DESIGN REQUIREMENTS:
- Use React Native Animated API or react-native-reanimated for smooth transitions (slide + fade between screens)
- Bottom progress dots indicator
- Next and Skip buttons on each screen (except last)
- Spring-based animations, never linear
- Color palette: soft off-white background (#FAF9F7), deep teal primary (#1A6B5A), warm amber accent (#F5A623), muted slate for text (#3D4A5C)
- Typography: large bold headings, relaxed body text, generous line-height
- Subtle grain/texture overlay on backgrounds using SVG
- Cards with soft layered shadows (not flat), slight border radius (16px+)
- Every interactive element has press states with scale feedback (Pressable with scale animation)
- Safe area insets handled properly via react-native-safe-area-context

FILE STRUCTURE:
- Main file: app/onboarding/index.tsx (or screens/OnboardingScreen.tsx if no app directory exists)
- Extract each slide into its own component in app/onboarding/slides/
- Shared animation hooks in app/onboarding/hooks/useSlideAnimation.ts
- Constants (colors, copy) in app/onboarding/constants.ts

ITERATION INSTRUCTIONS (follow every iteration):
1. Use Read and Glob tools to review what exists so far
2. Identify the single highest-impact improvement: animation smoothness, visual polish, component structure, missing screen, or accessibility
3. Implement that one improvement cleanly using Edit or Write tools
4. Use mcp__ide__getDiagnostics to check for TypeScript errors and fix any found
5. Continue until all 6 screens are complete, animations are smooth, design matches spec, and zero TS errors remain

QUALITY BAR (must meet before stopping):
- No placeholder TODO comments left in code
- All 6 screens fully implemented with real content
- Animations feel native and fluid (spring physics)
- Design is visually stunning — premium app quality
- All interactive elements have accessibilityLabel set
- Works on Android API 24+
- Zero TypeScript errors
